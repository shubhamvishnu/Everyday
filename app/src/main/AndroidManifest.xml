<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.everyday.skara.everyday">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".Everyday"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".BoardActivity" />
        <activity android:name=".NewNoteActivity" />
        <activity android:name=".LinkActivity" />
        <activity android:name=".TodoActivity" />
        <activity android:name=".AddBoardMembersActivity" />
        <activity android:name=".BoardMembersActivity" />
        <activity android:name=".OtherBoardsActivity" />
        <activity android:name=".UserAccountActivity" />

        <!-- <receiver android:name=".receivers.TodoReminderReceiver"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.BOOT_COMPLETED" /> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->
        <!-- <service android:name=".services.FirebaseInstanceIdService"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.google.firebase.MESSAGING_EVENT" /> -->
        <!-- </intent-filter> -->
        <!-- </service> -->

        <activity android:name=".ChatActivity" />
        <activity android:name=".TimelineActivity" />
        <activity android:name=".FinancialBoardActivity" />
        <activity android:name=".NewExpenseActivity" />
        <activity android:name=".PersonalFinancialBoardActivity" />
        <activity android:name=".SettingsActivity"></activity>
    </application>

</manifest>